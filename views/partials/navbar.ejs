<nav>
  <div class="logo-container">
    <a href="<%= (locals.user) ? '/folders' : '/login' %>">Strive</a>
  </div>
  <div class="nav-links">
    <% if (locals.user) { %>
    <a href="/logout">Logout</a>
    <% } else { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
    <% } %>
  </div>
</nav>
